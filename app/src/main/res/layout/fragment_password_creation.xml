<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center">

    <!-- TextView displays a heading prompting the user to create a password with bold text. -->
    <TextView
            android:id="@+id/tv_password_heading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Create a Password"
            android:textSize="18sp"
            android:layout_marginBottom="12dp"
            android:textStyle="bold" />

    <!-- EditText provides a field for users to input their password with a hint and secure text input. -->
    <EditText
            android:id="@+id/edt_password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter your password"
            android:inputType="textPassword"
            android:maxLines="1"
            android:layout_marginBottom="12dp" />

    <!-- Button allows the user to proceed after entering their password. -->
    <Button
            android:id="@+id/btn_continue"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Continue" />
</LinearLayout>
